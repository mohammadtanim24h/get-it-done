# Server
NODE_ENV=development
PORT=4000
API_PREFIX=/api
CORS_ORIGIN=http://localhost:3000

# Database (local development default; matches docker-compose.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/get_it_done?schema=public
